# CLAUDE

The graph's content, conforming to `../model/schema.yaml` once that
exists -- see `../CLAUDE.md` for the current status.

Seven empty files, one entity category each: `capabilities.yaml`,
`components.yaml`, `concepts.yaml`, `demos.yaml`, `features.yaml`,
`references.yaml`, `releases.yaml`. Populate after `../model/` is
written, not before -- these are meant to be validated against that
schema, not to define their own shape ad hoc.

Deferred pending a maintainer decision to start; see
`docs/planning/backlog.md` Part II.
